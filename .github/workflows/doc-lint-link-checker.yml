name: Docs lint/link checker

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: echo "Docs lint/link checker"
